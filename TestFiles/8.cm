/*
    8.cm
    Semantic Error (Conditions for if and while statements must be int Lines 10 and 11)
*/

void main(void)
{
    int x;
    if(x) output(foo());
    if(foo()) output(x, foo());
    while (foo())
    {
        x = x - 1;
    } 

}